"use client";

import { Container } from "@/components/layout/container";

export function HeroSection() {
    return (
        <section className="bg-[#F9FAFC] dark:bg-gray-900">
            <Container className="py-2">
                <div>{/* 히어로 섹션 내용이 필요하면 여기에 추가 */}</div>
            </Container>
        </section>
    );
}
